package ca.teamdman.sfm.common.util;

/// A singleton marker type used when a generic type parameter is required but no meaningful value is needed.
/// Similar to Void but can actually be instantiated.
public enum Unit {
    INSTANCE
}
